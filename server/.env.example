# GitHub personal access token with GitHub Models (inference) access.
# Used by LLMClient as apiKey for https://models.github.ai/inference
GITHUB_TOKEN=ghp_...

# PostgreSQL connection string — matches the docker-compose.yml defaults
DATABASE_URL=postgres://postgres:postgres@localhost:5432/tasks_ai

# Optional
# PORT=3000